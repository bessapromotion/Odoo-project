<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record model="ir.rule" id="crm_echeancier_rule">
        <field name="name">crm echeancier multi-company</field>
        <field name="model_id" ref="model_crm_echeancier"/>
        <field name="domain_force">['|',('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
    </record>

    <record model="ir.rule" id="order_paiement_rule">
        <field name="name">Ordre paiement multi-company</field>
        <field name="model_id" ref="model_ordre_paiement"/>
        <field name="domain_force">['|',('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
    </record>

</odoo>